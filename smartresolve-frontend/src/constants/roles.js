export const ROLES = Object.freeze({
  USER: "USER",
  SUPPORT: "SUPPORT",
  SUPERVISOR: "SUPERVISOR",
  ADMIN: "ADMIN",
});

export const ALL_ROLES = Object.freeze([
  ROLES.USER,
  ROLES.SUPPORT,
  ROLES.SUPERVISOR,
  ROLES.ADMIN,
]);